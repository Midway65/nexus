# Fork Divergence Registry

This file is the authoritative record of every file in `my-custom-branch` that intentionally
diverges from upstream (`ProfSynapse/nexus`). Load it at the start of every upstream merge
session to know which files require manual resolution and which can be auto-merged.

**Goal:** Full congruence with upstream wherever possible. Fork-specific additions should be
retired as soon as they are no longer needed or superseded by upstream. The only permanent
divergences are files that are fork-infrastructure by nature (deploy scripts, fork docs) or
that carry data migrations specific to this vault.

**Last audited against:** upstream/main HEAD (`92b4447a`) — v5.18.2 (tool operation receipts + reversible tooling #356, ownership-aware workspace/session deletes #347/#348, unified reasoning display #354/#357, states archive-flag denormalization #219, scoped semantic search #340, Groq history-in-continuations #368/#370, `content remove-property` #365, Claude 5 family, versioned tool-schema catalogue + build gate)  
**Audit date:** 2026-09-01  
**Next merge target:** next upstream/main HEAD

> ⭐ v5.18.2 merge notes (spans 5.17.0/.1/.2, 5.18.0/.1/.2, **49 commits**): 7 conflicts, and the first merge that made the fork *smaller*. **A TIER-3 DIVERGENCE RETIRED ITSELF.** Upstream `d3e473ba` (#358) independently reimplemented the fork's `getChatService` thunk after hitting the same bug (`ChatUIManager.registerViewEarly` constructs `ChatView` with a null chatService, copied by value into three dependency bags) — and their version is a **strict superset**: `() => ChatService | null`, guards at every consumer incl. `?.hasConfiguredProviders() ?? false`, plus tests (`ChatViewChatServiceWiring`, `ChatSubagentIntegration`). The fork's changes to those 4 files were verified thunk-only against the merge base, so all four resolved **take-theirs and are now byte-identical to upstream**. **SECOND SCHEMA RENUMBER, and the first to move two migrations at once:** upstream v15 (`tool_operation_receipts`) → **23**, v16 (`states.isArchived`) → **24**, `CURRENT_SCHEMA_VERSION` 22→24. Conflict shape held exactly as the v5.16.4 precedent predicted (single hunk on the version line; upstream's new blocks auto-merge out of order to the END of the array). Two things verified rather than assumed, either of which could have made the renumber unsafe: (1) v16's companion `StateRepository.backfillDerivedStateMetadata()` is **data**-gated (`WHERE isArchived IS NULL`, called unconditionally at `HybridStorageAdapter.ts:409`), not version-gated; (2) v16 is the first renumbered migration whose DDL is **not** pure `IF NOT EXISTS` — it carries a bare `ALTER TABLE states ADD COLUMN isArchived INTEGER`, safe only because `runMigrations()` regex-matches ADD COLUMN and skips it when `columnExists()` is true (`SchemaMigrator.ts:716-724`). **FIXED A LATENT BUG THE v5.16.4 RENUMBER MISSED:** `schema.ts` stamped fresh installs with a literal `14` against a MAX of 22 (upstream's own header comment step 4 warns a stale literal makes new users replay migrations, and no machine with an existing cache can see it) — now 24, with a **STEP 5** added to the convention block so the next renumber cannot miss it. Also documented in the convention: `runMigrations()` does a plain `filter` and iterates in **array order with no sort**, which is *why* renumbering trailing blocks in place is correct. Test file taken `--theirs` then remapped 11→19/12→20/13→21/14→22/15→23/16→24: **18/18 green** (was 11). **No new dependencies** (dep graph byte-identical, 768 packages, 0 version differences); TS stays ^6.0.3; `package-lock.json` kept at upstream's tab formatting — local npm 11.19 rewrites it to 2-space, a 21k-line formatting-only churn that was **reverted** to avoid a permanent divergence (re-revert after any future `npm install`). **CLAUDE.md did not conflict** for the first time in 10 merges (upstream untouched since the base). New build gate `npm run schemas:check` runs FIRST in `build` and **PASSES** (5.18.2, 80 CLI tools, 2 MCP). `lint:mobile` clean (404 modules). Full suite **4979 passed / 0 failed**; the long-standing `ModelAgentManager.test.ts` failure is gone. ⚠️ **`minAppVersion` 1.8.7 → 1.10.0** (Bases API) — deploy target must be on Obsidian ≥ 1.10.0. ⚠️ Five Groq models pruned, Groq default → GPT-OSS 120B. **Code divergence surface 13 files → 9.** **DEPLOYED 2026-09-01** to the Linux vault (Obsidian 1.13.7, past the new 1.10.0 floor); `npm run deploy` is Windows-only (postbuild.ps1 hardcodes `C:\Users\middl\...`) so the copy was manual, with the replaced artifacts backed up in-vault under `backup-5.17.2-20260901-153655/`. ⚠️ **The vault was running 5.17.2 from a lineage absent from this git history** — its build has migrations 3-22 contiguous, i.e. no 12-16 gap and no `[upstream vN]` prefixes, so it is neither this branch nor stock upstream (which stamps 16, not 22). Probably a Windows-side 5.17.x merge that never reached this checkout; worth reconciling. Deploy was safe regardless because migration 22 is identical in both builds, so the DB is at 22 and only 23+24 run — the fork's v17-v19, which DROP the embedding-metadata tables, stay below the line and do not re-fire. On any machine whose installed build is unknown, check that before deploying: a stock-upstream DB at 14/16 WOULD trigger those drops. See `docs/plans/upstream-merge-v5.18.2-plan.md` §9.

> ⭐ v5.16.4 merge notes (spans 5.16.2/.3/.4 + post-tag, **57 commits — most significant merge in fork history**): 4 conflicts. **THE FORK'S FIRST SCHEMA RENUMBER EXECUTED.** Upstream shipped their first new migration since divergence (**v14**, notes/note_properties into the owned schema) → renumbered to **fork v22**, `CURRENT_SCHEMA_VERSION` 21→22, per the fork's own FORK MIGRATION NUMBERING CONVENTION. Verified necessary: the migrator filters `m.version > currentVersion` with **no sort**, so at DB v21 upstream's v14 would have been **silently skipped forever** — the very failure their commit describes (`SQLite3Error: no such table: notes` after a cache rebuild). v22 DDL is upstream's verbatim, additive, all `IF NOT EXISTS`; the tables already exist on this vault (runtime `ensureSchema` since v5.12.2) so it applies as a **no-op that formalizes ownership**. Companion `SCHEMA_SQL` change in `schema.ts` auto-merged. Tests rewritten to the renumber: **11/11 green** (was 8), incl. a "runs only v22 when starting at v21" case proving it will fire on this install. Other conflicts: CLAUDE.md kept-ours (upstream stripped theirs 3809→~560 words moving procedural knowledge into `.claude/skills/` + `.skills/` — those arrive as new files regardless; adopting their slim version is a deferred follow-up), package-lock took-theirs then npm-regenerated. **NEW RUNTIME DEP: `defuddle@0.19.2`** (#338 web capture) — mobile-safe, loaded via dynamic `import()` in WebContentExtractor, never top-level. `eslint.config.mjs` auto-merged (upstream added mobile-safety blocklists for issue #221); fork's JSONLWriter exemption intact. **`ChatView.ts` untouched this release** (breather after 4 straight). New build gate `lint:mobile` (mobile import reachability) **PASSES**: "clean: no Node built-in is statically reachable from init" (397 modules) — the fork's JSONLWriter `require()`s are runtime-lazy so they don't trip it (still the same 2 non-blocking warnings). Startup-hydration fix (`7a5428c0`) repairs a rebuild leaving hydration at `running` forever, timing out every `waitForQueryReady()` caller — **same symptom family this vault has hit historically → net-positive**. Build green under TS6.

> v5.16.1 merge notes (spans 5.16.0+5.16.1): routine — 1 conflict (CLAUDE.md kept-ours). One divergence-relevant auto-merge verified safe: **`ChatView.ts`** (voice, 1 commit) — upstream edits at lines 695/947, far from fork thunk (123/143/161); thunk verified count=3 + tsc clean. This is the **4th touched release** the thunk has survived (voice v5.11.x, ticker v5.13, v5.15, voice v5.16). **No SchemaMigrator renumber** (upstream still v13; fork v21). **TS stays ^6.0.3**; **no new deps** (voice batch reuses existing infra; all models additive, whisper-1 + vault models intact). Build green under TS6 (same 2 non-blocking JSONLWriter warnings). connectorContent.ts + cliAssets.ts regen matched upstream (git-clean). #304 OpenRouter OAuth = label-only fix in ProvidersTab.ts (no fork divergence; existing auth unaffected). #303 searchWorkspaces auto-load is opt-in. npm audit dev-tree advisories carried, NOT applied.

> v5.15.4 merge notes (spans 5.15.3+5.15.4): routine — 1 conflict (CLAUDE.md kept-ours), **ZERO divergence-file touches**. **No SchemaMigrator renumber** (upstream still v13; fork v21). **TypeScript stays ^6.0.3** (TS6 settled last merge — no repeat build gate). **No new deps** (Realtime 2.1/Kimi K3/Voxtral reuse existing infra; models purely additive, whisper-1 + all vault models intact). Build green under TS6 (same 2 non-blocking JSONLWriter `obsidianmd/no-nodejs-modules` warnings as v5.15.2). `cliAssets.ts` + `connectorContent.ts` regenerated to match upstream exactly (git-clean). CLI transport fixes touch `cli/` (no fork divergence). npm audit set shifted again → @hono/node-server + body-parser + brace-expansion (all dev-tree/eslint-plugin transitive, not shipped) — NOT applied. **NOT deployed this cycle (user deferred deploy).**

> v5.15.2 merge notes (spans 5.14.1→5.15.2, 18 commits — biggest since voice batch): 2 conflicts (CLAUDE.md kept-ours; package-lock.json took-theirs then npm-install-regenerated for TS6). **TypeScript 6 migration (#285)** `^5.9.3`→`^6.0.3` — build VERIFIED GREEN under tsc 6.0.3 (thunk, JSONLWriter, SchemaMigrator v21, SettingsView guard all compile). **1 fork fix required:** upstream's hardened lint (`eslint-comments/require-description`, active via the vault-path security phases) flagged the fork's inline eslint-disables in `JSONLWriter.readEventsStreaming` → added descriptions (matches upstream's own #831c3617/#3e94d14c pattern); 2 residual non-blocking `obsidianmd/no-nodejs-modules` warnings on the desktop-only Node requires (build passes). **eslint.config.mjs** auto-merged (upstream added CLI ignores `nexus-cli.js`/`cli/`, `prefer-setting-definitions` off, Codex brand, `no-nodejs-modules` off for .mjs); fork's JSONLWriter exemption (line ~156) intact. **ChatView.ts** thunk survived (count=3, 3rd consecutive touched release). **No SchemaMigrator renumber** (upstream still v13). **Storage cutover (#d3b65e8f):** post-`verified` migration now drops legacy read base paths (`[]`) to prevent deleted-data resurrection/cloud-sync races — SAFE for this vault (data at verified `00-System/Nexus/Data`; rootPath resolvers untouched → reset risk LOW). CLI bridge (#287) desktop-only. connectorContent.ts NOT divergent (regen matched). npm audit: hono/js-yaml dev-tree advisories carried, NOT applied.

> v5.14.0 merge notes (spans 5.13.2+5.14.0): trivial — 1 conflict (CLAUDE.md kept-ours), **ZERO divergence-file touches** (smallest merge in the series). **No SchemaMigrator renumber** (upstream still v13; fork v21). **No new deps** (native OCR reuses existing HTTP/pdfjs infra). package-lock = version field only. Two additive features: native Mistral OCR/PDF-image-extraction (#0bf465f1, improves the vault's mistral-ocr ingest path, fixes truncation) + Claude Sonnet 5 (#18534243, additive catalog entry; vault defaults claude-opus-4-7/claude-sonnet-4-6 untouched). ⚠️ **npm audit set CHANGED**: esbuild advisory no longer surfaced; now 1 HIGH (hono) + 1 MODERATE (js-yaml) — both transitive in the DEV/tooling tree (js-yaml under @istanbuljs/load-nyc-config coverage tooling; hono's vulnerable serve-static/Lambda/CORS paths not exercised by an Obsidian plugin), NOT shipped; upstream has identical dep set → identical advisories; NOT applied per standing no-`audit fix` rule. Full `npm run build` green.

> v5.13.1 merge notes (spans 5.13.0+5.13.1): 1 conflict (CLAUDE.md kept-ours). Two divergence-relevant auto-merges, both verified safe by base→upstream region analysis: (1) **`ChatView.ts`** — upstream's #279/#280 working-ticker fixes add a new `WorkingIndicatorController` at lines 32/92/627, OUTSIDE the fork's thunk region (121/141/159); thunk verified count=3 + `WorkingIndicatorController` 10 refs coexisting + tsc clean. (2) **`eslint.config.mjs`** — upstream #3e94d14c extended the upstream-owned sentence-case block (acronyms +KV/MTP/GLM, ignoreRegex +`^OLLAMA_`); fork's JSONLWriter exemption (different region) intact. **No SchemaMigrator renumber** (upstream still v13; fork v21). **No new deps** (local-model features are native HTTP integrations). package-lock = version field only (tab-indent settled in v5.12.2, as predicted). esbuild HIGH advisory still carried (not applied). `ChatView.ts` thunk has now survived 2 consecutive upstream-touch releases (v5.11.x voice, v5.13 ticker) — strengthens case to propose thunk upstream. Full `npm run build` green.

> v5.12.2 merge notes: 1 conflict (CLAUDE.md kept-ours). **`ChatSendCoordinator.ts` was edited by upstream (#276 chat fix) for the FIRST time** — the fork's `getChatService` thunk (3 sites) auto-merged cleanly (different region) and was VERIFIED count=3 + tsc-clean post-merge. It now joins `ChatView.ts` as a confirmed live-overlap thunk-carrier → consider proposing the thunk upstream to retire the divergence. **No SchemaMigrator renumber** (upstream still v13; fork stays v21; #274 notes-index uses in-memory `CREATE TABLE IF NOT EXISTS` sql.js tables, NOT a persisted migration). **No new deps** (package set verified identical; notes-index reuses bundled WASM SQLite). ⚠️ **package-lock.json reformatted spaces→tabs** (~10.5k symmetric whitespace churn) because upstream switched package.json to tab indentation and npm regenerated the lockfile to match; dep content unchanged; upstream's own lockfile is STALE (5.11.1) so not a clean target. agy (#278) is text-completion-only (no tools/streaming) but user's google-gemini-cli provider is disabled+unauth → no impact. esbuild HIGH advisory still carried (not applied).

> v5.12.1 merge notes: routine — **1 conflict only** (CLAUDE.md kept-ours). `connectorContent.ts` FF'd clean (regen timestamp-only). **Zero divergence-file touches** — every Tier 2/3 file auto-merged untouched. **No SchemaMigrator renumber** (upstream still v13; fork stays v21). **No dependency changes** (version bump only). Only user-visible change is the model-catalog prune (#268): removed Claude 4.5 Opus/Sonnet, GPT-5/5.1/Mini/Nano, Gemini 2.5 & 3.0-Preview, old Groq Llama/Gemma; added GLM 5.2 + Kimi K2.7 Code. **Verified NIL user impact** — all of this vault's configured models survive (default `claude-opus-4-7`, agent `claude-sonnet-4-6`, image `gemini-2.5-flash-image` [separate catalog], video/OCR/transcription/webllm all untouched). esbuild HIGH advisory (GHSA-gv7w-rqvm-qjhr) still carried — build-time devDep, not shipped, NOT applied. Full `npm run build` green.

> v5.12.0 merge notes: cleanest merge in the series — **1 conflict only** (CLAUDE.md kept-ours). `connectorContent.ts` no longer divergent (converged v5.11.2) → fast-forwarded clean; regen timestamp-only. **Zero divergence-file touches by upstream** — every Tier 2/3 file auto-merged untouched. **No SchemaMigrator renumber** (upstream still v13; fork stays v21). **No dependency changes** (version bump only). Headline #265 "dreaming embeddings" (query-side low-rank adapter over frozen MiniLM, idle-time dream consolidation) ships IDENTITY by default + kill-switch + NO schema migration; persists to `<root>/data/embeddings/adapter.json` (→ `00-System/Nexus/Data/embeddings/adapter.json`, absent=identity). #267 fixes empty-task-board-on-cold-start. ⚠️ `npm audit` now reports 1 HIGH on esbuild (GHSA-gv7w-rqvm-qjhr) — **build-time-only devDep, not shipped; upstream pins same version (not a merge regression); fix is breaking → NOT applied** per standing no-`--force` rule. Full `npm run build` green.

> v5.11.2 merge notes: 2 conflicts only (CLAUDE.md kept-ours, connectorContent.ts took-theirs; regen matched upstream byte-for-byte). All Tier 2/3 divergences auto-merged. **No SchemaMigrator renumber** (upstream still v13; fork stays v21). **RETIRED the `version-bump.mjs` eslint ignore** (`5ca9487a`): upstream `c47d00d7` disabled obsidianmd typed rules for all `.js`/`.mjs` files, superseding the fork's `da5d7072` workaround one release later — `eslint.config.mjs` divergence now back to just the JSONLWriter exemption. **HybridStorageAdapter split** (#261/#262/#263) extracted ~370 lines into HybridStorageAssembly + StorageMaintenanceService; auto-merged (fork fully converged this file); path resolution untouched → data-folder reset risk LOW, but it's the adapter behind past `waitForQueryReady` hangs → storage smoke matters. Secure key storage (#254) is opt-in/default-off — keys stay in data.json. 0 vulnerabilities. Full `npm run build` green.

> v5.11.1 merge notes: 2 conflicts only (CLAUDE.md kept-ours, connectorContent.ts took-theirs+regen). All Tier 2/3 divergences auto-merged. **No SchemaMigrator renumber** (upstream still v13; fork stays v21). `ChatView.ts` (Tier 3 `getChatService` thunk) auto-merged but is NOW a live-overlap file — v5.11 voice features edit `src/ui/chat/` heavily, so the "upstream never touches chat" assumption is retired. Dependency slimming dropped winston/uuid/web-llm/transformers/express/cors etc. → lockfile 854→710 packages, **0 vulnerabilities**. Known: `obsidianmd/no-plugin-as-component` typed rule throws on `version-bump.mjs` — **pre-existing upstream issue, reproduces on pristine upstream checkout**, not caused by this merge. Fixed `da5d7072` by adding `version-bump.mjs` to eslint.config.mjs ignores (new small Tier-2 divergence; upstream-eligible). Full `npm run build` now green.

---

## Tier 1 — Always conflict on upstream merge

These files contain fork-specific additions that upstream will never have. Every upstream merge
requires manual resolution using the pattern: accept upstream base, then layer back the fork additions.

*No Tier 1 entries.* The action bar feature (MessageBubble.ts integration, MessageActionBar.ts,
CreateFileModal.ts) was deprecated and removed in the v5.8.0 merge (2026-04-18). MessageBubble.ts
now tracks upstream exactly.

---

## Tier 2 — Conflict only when upstream touches them

These files have fork additions that are self-contained. Upstream rarely touches them, but when
they do a conflict will occur. Resolution is always: take upstream base, then restore the fork block.

| File | Fork change | Fork block to restore |
|------|-------------|----------------------|
| `src/database/schema/SchemaMigrator.ts` | Convention comment + fork migrations v17–v19 (v12–v16 removed 2026-04-08) + v20 (upstream v12 `shard_cursors`, 2026-05-07) + v21 (upstream v13 `skills`, 2026-05-22) + v22 (upstream v14 `notes`/`note_properties`, 2026-08-14) + **v23 (upstream v15 `tool_operation_receipts`) and v24 (upstream v16 `states.isArchived`), both merged 2026-09-01**. `CURRENT_SCHEMA_VERSION = 24`. | Restore convention comment block + migrations v17–v24. Upstream is at v16; they need **6 more** before their counter exceeds the fork MAX and renumbering can stop. **Worked example: the v5.18.2 merge renumbered TWO at once (v15→23, v16→24) — conflict is only on the `CURRENT_SCHEMA_VERSION` line; upstream's new blocks auto-merge to the END of the array out of order, so renumber them in place, prefix the description `[upstream vN]`, add a renumber comment, and DO NOT reorder the file** (`runMigrations()` filters and iterates in array order with no sort — array order IS execution order). **Then do STEP 5: bump the fresh-install stamp in `schema.ts` to match.** |
| `src/database/schema/schema.ts` | Fresh-install stamp `INSERT OR IGNORE INTO schema_version VALUES (24, ...)` + `Current Version: 24` header, carrying fork numbering instead of upstream's 16. Added 2026-09-01 (v5.18.2) after discovering the v5.16.4 renumber left it at a stale `14` against a MAX of 22. | Take upstream's file, then re-apply the fork's `CURRENT_SCHEMA_VERSION` value to the stamp literal and the header comment. Conflicts whenever upstream bumps their own stamp — i.e. every time they ship a migration, which is also every time a renumber is due. Retires automatically once upstream's counter passes the fork MAX and renumbering stops. |
| `src/database/storage/JSONLWriter.ts` | `readEventsStreaming()` private method using Node.js `readline` for streaming large JSONL files. Marked retired 2026-04-19 prematurely — method is still present. **Retirement candidate (Phase 4)**: verify no callers, then remove method and take upstream exactly. |
| `eslint.config.mjs` | `"src/database/storage/JSONLWriter.ts"` added to Node.js modules exemption list. Required as long as JSONLWriter.ts carries `readEventsStreaming()`. **Retire together with JSONLWriter.ts.** |

---

## Tier 3 — Fork bug fixes (low conflict risk, but track for awareness)

| File | Fork change | Notes |
|------|-------------|-------|
| `src/settings/SettingsView.ts` | `availableUpdateVersion` stale-clear guard in `renderHeader` (commit `a4922ef4`, 2026-04-28). | As of v5.9.x upstream removed the in-plugin auto-updater and the "Update" button just opens the GitHub release page in a browser — `availableUpdateVersion` is still persisted but never cleared after the user installs, so this guard is universally useful now (not just for direct-deploy users). More upstream-eligible than ever. |
| `tests/unit/SchemaMigrator.test.ts` | Numeric assertions track the v20/v21/v22/**v23/v24** renumbers (upstream tests them at v12–v16). Five describe blocks mirroring upstream's, all renumbered; `CURRENT_SCHEMA_VERSION` asserted at 24; "starting at v19" expects applied=5/toVersion=24; "starting at v22" expects v23+v24; no-op fixture seeds MAX=24. **18 tests.** (last touched in the v5.18.2 merge, 2026-09-01) | Resolution recipe that worked twice now: `git checkout --theirs`, then remap upstream's numbers onto it (11→19, 12→20, 13→21, 14→22, 15→23, 16→24) rather than hand-merging hunks. Watch for numbers the obvious regexes miss — a bare `c.params[0] === 13` stamp check and the `for (const v of [...])` loop are not `m.version === N` — and protect issue references like `#218`/`#219` from a blind numeric substitution. The v24 block's additive-only assertion must allow `ADD COLUMN`; the earlier blocks' must not. |

**Retired entries:**
- `src/ui/chat/ChatView.ts` + `src/ui/chat/services/{ChatSendCoordinator,ChatSessionCoordinator,ChatSubagentIntegration}.ts` — **RETIRED 2026-09-01** (v5.18.2 merge): upstream `d3e473ba` (#358) landed the same lazy-`getChatService` fix independently, having hit the same reload bug, and did it *better* — the accessor is typed `() => ChatService | null` with null guards at every consumer (not just `ChatSubagentIntegration`), plus regression tests the fork never had. The fork's diff against the merge base was thunk-only, so there was nothing to layer back: took upstream wholesale, all four files now byte-identical. This is the outcome the entry had been flagged for since 2026-06-03 ("upstream-eligible — propose upstream or retire"); it retired without the fork having to propose anything.
- `tests/unit/ModelAgentManager.test.ts` — **RETIRED 2026-05-13** (v5.9.3 merge `f4fe29f1`): upstream switched the assertion from strict `toHaveBeenCalledWith({...})` to `expect.objectContaining({...})`, which tolerates the fork's extra `imageProvider`/`imageModel`/`transcriptionProvider`/`transcriptionModel` fields without adjustment. Took upstream entirely.
- `src/database/adapters/HybridStorageAdapter.ts` — **RETIRED 2026-04-20** (commit `ebd13dba`): pre-migrated 59 files/22,872 events to vault-root via `scripts/migrate_to_vault_root.js`, then took upstream exactly. All 5 patches removed. Data now in `00-System/Nexus/data/`.
- `src/ui/chat/builders/ChatLayoutBuilder.ts` — **RETIRED 2026-04-20** (commit `879bfede`): took upstream exactly; banner restored, `_component` rename removed
- `src/settings/tabs/WorkspacesTab.ts` — **RETIRED 2026-04-20** (commit `879bfede`): took upstream exactly; upstream v5.8.2 has own race-condition mitigation, `waitForReady()` patch no longer needed
- `src/settings.ts` — **RETIRED 2026-04-20** (commit `879bfede`): took upstream exactly; stale badge fix removed (upstream users not reporting this issue)
- `src/agents/searchManager/services/MemorySearchProcessor.ts` — **RETIRED 2026-04-19**: upstream dropped `?.` guards (v5.8.1); taking upstream version
- `src/agents/toolManager/services/ToolBatchExecutionService.ts` — **RETIRED 2026-04-19**: upstream dropped `workspace.name?.` guard (v5.8.1)
- `src/services/WorkspaceService.ts` — **RETIRED 2026-04-19**: upstream dropped `name ?? ''` and `name?.` guards (v5.8.1)
- `src/components/shared/ChatSettingsRenderer.ts` — **RETIRED 2026-04-19**: upstream added `void` back (v5.8.1); taking upstream version
- `src/ui/chat/components/MessageActionBar.ts` — **RETIRED 2026-04-18**: action bar feature deprecated and removed in v5.8.0 merge
- `src/ui/chat/components/CreateFileModal.ts` — **RETIRED 2026-04-18**: action bar feature deprecated and removed in v5.8.0 merge
- `src/ui/chat/components/MessageBubble.ts` — **RETIRED 2026-04-18**: no longer fork-divergent after dropping action bar
- `styles.css` — **RETIRED 2026-04-15**: no fork CSS divergence; action button styles absorbed by upstream
- `src/database/storage/JSONLWriter.ts` — **INCORRECTLY RETIRED 2026-04-19**: method was never removed from fork. Re-listed as active Tier 2 divergence 2026-04-24.
- `eslint.config.mjs` — **INCORRECTLY RETIRED 2026-04-19**: exemption was never removed from fork. Re-listed as active Tier 2 divergence 2026-04-24.
- `src/agents/searchManager/services/MemorySearchProcessor.ts` — **RE-RETIRED 2026-04-24** (commit `2ecafad2`): `?.` guards re-introduced after v5.8.1 retirement; removed again to match upstream.
- `src/agents/toolManager/services/ToolBatchExecutionService.ts` — **RE-RETIRED 2026-04-24** (commit `2ecafad2`): same as above.
- `src/ui/chat/components/BranchHeader.ts` — **RETIRED 2026-04-24** (commit `2ecafad2`): verbose JSDoc trimmed to match upstream's single-line form.
- `src/database/storage/SQLiteMaintenanceService.ts` — **RETIRED 2026-04-24** (commit `2ecafad2`): trailing blank line removed to match upstream.
- `src/settings/tabs/ProvidersTab.ts` — **RETIRED 2026-04-09**: superseded by upstream PR #126
- `src/components/LLMProviderModal.ts` — **RETIRED 2026-04-09**: superseded by upstream PR #126
- `src/database/repositories/MessageRepository.ts` — **RETIRED 2026-04-09**: superseded by upstream PR #123
- `src/ui/chat/components/ContextProgressBar.ts` — **RETIRED 2026-04-15**: file deleted by upstream PR #131

---

## Special case — connectorContent.ts

`src/utils/connectorContent.ts` is generated during build (timestamp in header). It should be
**reset to upstream before every merge** with:

```
git restore src/utils/connectorContent.ts
```

Do not treat timestamp-only diffs as fork divergences.

---

## How to use this file

1. Before each upstream merge, run: `git diff --name-status upstream/main..my-custom-branch`
2. Cross-reference each modified file against this registry
3. Files not listed here should match upstream exactly — investigate any that don't
4. After resolving conflicts, re-run the diff to confirm no unintended divergences remain
5. If new fork additions are made, add them to this file before committing

---

## Post-deploy checklist

After every `npm run deploy` + Obsidian reload:

1. Open the Nexus UI — workspaces and conversations should appear within ~5 seconds
2. If the UI shows only "Default" workspace or is empty, **do not reload**. Wait 3–4 minutes for fullRebuild to complete in the background.
3. If still empty after 4 minutes, the sync_state loop has re-triggered. Run the repair script with Obsidian closed:
   ```
   node scripts/repair_sqlite_cache.js
   ```
   Then reopen Obsidian. Data appears immediately (no wait).

**Root cause of the loop:** `sync_state` empty in `cache.db` → `fullRebuild` → `clearAllData()` → rebuild interrupted → `sync_state` never written → same loop on next startup. The JSONL source files are never touched; only SQLite is affected.

**All vault data is in plugin-scoped storage** (`.obsidian/plugins/nexus/data/`). The `00-System/Nexus` path in the Nexus Data tab is ignored by the fork patches. `Nexus_vault_root_REVIEW/` and `NEXUS/` in the vault root are stale backup folders — confirmed up-to-date with plugin-scoped as of 2026-04-20.
